import { Mail, MapPin, Phone } from 'lucide-react';
import { PageHero } from '@/components/PageHero';
import { ContactForm } from '@/components/Forms';
import { siteConfig } from '@/lib/site';
export const metadata={title:'Contact'};
export default function Contact(){return <><PageHero eyebrow="Contact" title="Start a conversation with Waseb Amanat Foundation." description="For partnerships, community needs, volunteering, donations or program questions, send a message through the secure contact form."/><section className="section"><div className="container contact-grid"><div className="contact-info"><h2>Contact information</h2><p>Replace the placeholder contact details with verified foundation information before launch.</p><div className="contact-line"><MapPin/><div><strong>Address</strong><p>{siteConfig.address}</p></div></div><div className="contact-line"><Phone/><div><strong>Phone</strong><p>{siteConfig.phone}</p></div></div><div className="contact-line"><Mail/><div><strong>Email</strong><p>{siteConfig.email}</p></div></div>{process.env.NEXT_PUBLIC_MAP_EMBED_URL?<iframe title="Waseb Amanat Foundation location" src={process.env.NEXT_PUBLIC_MAP_EMBED_URL} style={{width:'100%',height:260,border:0,borderRadius:22}} loading="lazy" referrerPolicy="no-referrer-when-downgrade"/>:<div className="map-placeholder"><div><MapPin size={34}/><strong>Google Maps embed area</strong><p>Add NEXT_PUBLIC_MAP_EMBED_URL after confirming the office location.</p></div></div>}</div><ContactForm/></div></section></>}
